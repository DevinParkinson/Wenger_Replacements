<h1 class="center" style="color: white"> Possible Wenger Replacements </h1>
<h5 class="center" style="color: white">
  Here are a list of possible replacements and their chances (1-10) for taking over for Arsene.
</h5>
<% @replacements.each do |replacement| %>
  <div class="center card red darken-4 row">
    <div class="col s12">
      <div class="card-action">
    <h3><%= replacement.name %></h3>
    <h4>Rating: <%= replacement.rating %><h4>
      <h4> Chance of being next manager: <%= replacement.likelihood %> </h4>
  <a class="btn-flat" <%= link_to 'Credentials', replacement_path(replacement)%></a>
  <a class="btn-flat" <%= link_to 'Edit', edit_replacement_path(replacement) %> </a>
  <a class="btn-flat" <%= link_to 'Remove', replacement, method: :delete %> </a>
  <% end %>
      </div>
    </div>
  </div>
  <a class="btn-flat" <%= link_to 'Enter a New Candidate', new_replacement_path %> </a>
